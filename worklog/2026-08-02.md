# 2026-08-02 工作日志

## 工作记录

- 本次使用 `zju-server`。对旧 VLM checkpoint 完成 13 个 held-out episode、1530 帧的生成式评测：route 准确率 `98.82%`，新 subtask exact accuracy `0%`。旧模型仍输出自然语言旧标签，不能直接作为新 action 训练的冻结条件模型。
- 新增 `vlm_relabel` 阶段：仅迁移旧 checkpoint 的 Qwen VLM，按当前 `nav_turn/nav_straight/nav_stop/arm_approach/arm_contact/arm_retreat` 标签完整训练一轮；复测通过后再分别训练 NAV 和 GRASP/PLACE 的 10 维 flow-matching action head。启动器默认关闭 CPU offload。
- 两卡 A40 在 ZeRO-3 分片初始化时 OOM；四卡混合 GPU 可完成分片，但 AdamW foreach 在首个 step 额外申请 `3.73 GiB` 导致 3090 OOM。为 Go2 配置显式关闭 `optimizer.foreach`，改为逐参数更新以降低峰值显存，其他配置保持原默认行为。

## 全日总结

- 完成旧 VLM 在当前 held-out 数据上的真实生成评测；route 基本可用，subtask 标签体系不兼容。
- 完成旧 VLM 到当前 10 维模型的安全部分加载与 `vlm_relabel` 训练入口；针对四卡首 step 峰值显存关闭 Go2 AdamW foreach，相关本地测试 `13 passed`。
- 远程训练仅使用空闲 GPU 3、4；后续 action 阶段以 VLM 新标签复测通过为前置条件。
