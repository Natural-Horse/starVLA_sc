# 2026-08-02 工作日志

## 工作记录

- 完成 n200 训练语义重置：全局 instruction 增加 box1/box2 八方向；VLM subtask GT 改为逐帧完整局部 instruction；六类分段标签仅保留为 `phase_label`。旧数据由 loader 动态补全，新数据校验已写入的方向，避免改写 Parquet 和重复拼接。
- n200 全量审计：130 episode、30200 原始帧、16170 个 stride 后训练样本、11 种全局方向组合、14 种局部 instruction，全部可加载。Go2 数据/VLM/配置/协议测试 `26 passed, 3 skipped`。
- 新增 `vlm_instruction` 完整 epoch tmux 入口，生成式评测上限由 24 调为 64 token，训练和服务 prompt 均要求输出完整局部 instruction。新 VLM 微调通过 held-out exact 评测后方可启动 action 阶段。
- 本次使用 `zju-server`。对旧 VLM checkpoint 完成 13 个 held-out episode、1530 帧的生成式评测：route 准确率 `98.82%`，新 subtask exact accuracy `0%`。旧模型仍输出自然语言旧标签，不能直接作为新 action 训练的冻结条件模型。
- 新增 `vlm_relabel` 阶段：仅迁移旧 checkpoint 的 Qwen VLM，按当前 `nav_turn/nav_straight/nav_stop/arm_approach/arm_contact/arm_retreat` 标签完整训练一轮；复测通过后再分别训练 NAV 和 GRASP/PLACE 的 10 维 flow-matching action head。启动器默认关闭 CPU offload。
- 两卡 A40 在 ZeRO-3 分片初始化时 OOM；四卡混合 GPU 可完成分片，但 AdamW 在首个 step 为每个 rank 的 `3.73 GiB` 展平分区生成 `sqrt` 临时量，foreach 和 single-tensor 路径均会导致 3090 OOM。Go2 配置改用 `AdamW(fused=True)` 原位 CUDA kernel 降低峰值，其他配置保持原默认行为。
- 发现 `go2_n200_vlm_relabel_a40x2_b1_fused_0802` 仍以六类 phase 标签为目标；在 step 1484 正常中断并关闭其 tmux。远端拉取 `42381bb` 后从干净的 `...0801_vlm/final_model` 启动 `go2_n200_vlm_instruction_a40x2_b1_0802`：A40 3、4，batch 1/GPU，ZeRO-3、fused AdamW、无 CPU offload，7275 step，每 1000 step 保存，TensorBoard 6014。已通过首步峰值并稳定运行，约 2.16 秒/step。

## 全日总结

- 完成全局方向与局部 instruction 新语义、真实 n200 全量审计、VLM 训练/评测入口和在线 prompt 对齐。
- 保留 ZeRO-3、fused AdamW、无 CPU offload 配置；后续仅在新 VLM held-out 生成式评测通过后训练 NAV 与 GRASP/PLACE action。
- 本地相关测试 `26 passed, 3 skipped`；提交 `42381bb` 已推送并同步 zju-server，新目标 VLM 完整 epoch 正在 tmux 稳定训练。
