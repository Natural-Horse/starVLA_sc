# 2026-08-05 工作日志

## 工作记录

- r3 归一化+RTC 训练完成：`go2_n200n250_action_norm_h10_rtc_5a40_0804_r3`
  （4881 步 / 5 epoch / ~4.3h），final_model 已保存。
- 推理 server 切 final_model 复测（GPU 6 / 3090）：route 7/7、subtask 语义正确、
  locked ~0.59s、RTC 钉帧验证通过；arm 较 steps_1000 更贴 GT 但精度仍不足。
- waypoint 可用性量化（120 帧 locked nav）：step0 落在数据范围的比例 **0/120**，
  根因 pooled 归一化把 step0 压进窄区间、采样 clip 后反归一化成极值。
- 双 expert 动作头开发（`robodog_two_heads` 分支）：QwenPI 改 DualActionHeads
  （nav 3 维 / arm 7 维，route token 选路），eval 冒烟通过。
- 关节/动作审计：全部关节在 X5 限位内；joint5（=真机 X5 joint5，腕部自由度）
  恒定 0，属任务特性；步间速度合理。抓取探测：arm 目标 100% 在 workspace，
  但首个 TCP 目标中位误差 0.171m，当前模型不宜实机抓取。
- **夹爪语义修复**：训练目标夹爪维原为实测指位（`lerobot_dataset.py` 关节均值），
  抓握时停在物体宽度 0.77；改用 `control.action` 夹爪指令（0=闭,1=开）。
  指令分布非二值：23% 闭合 / 35.7% 过渡 / 41.3% 张开。
- **按 step 索引归一化**：`dataset_statistics.json` 的 q01/q99 改为
  `[horizon,10]`，避免 waypoint 首步被跨 step 合并统计压坏；后端反归一化
  按步查表（兼容旧 pooled）。
- 双 head 训练启动排障（r4/r5/r6）：GPU 0/1 被 xch 占用→显式 2,3,4；
  AccessTrackedConfig 与 OmegaConf 兼容；NCCL 600s 超时（数据 worker 卡死）。
- **回退单 head 训练**：双 head 引入过多问题，robodog 分支保留全部数据修复
  （per-step + 夹爪指令）启动 `go2_singlehead_perstep_rtc_3a40_0805_r7`：
  A40 2/3/4、b16/卡(48/步)、4068 步（5 epoch）、RTC + horizon10。

## 全日总结

- 定位并修复两个数据问题：①waypoint pooled 归一化→per-step；②夹爪目标用实测
  指位→control.action 指令。均已移植到 robodog 单 head 分支（fc43a94）。
- r7 单 head 训练已在 A40 2/3/4 启动（b16，~5h）；双 head 分支保留待评估。
- r3 模型 waypoint 0/120 不可用、抓取中位误差 17cm 不宜实机；等 r7 完成后再闭环验证。
