# 2026-08-05 工作日志

## 工作记录

- r3 归一化+RTC 训练完成：`go2_n200n250_action_norm_h10_rtc_5a40_0804_r3`
  （4881 步 / 5 epoch / ~4.3h），final_model 已保存，含 steps_1000~4000 中间 checkpoint。
- 推理 server 切换 final_model 并复测（GPU 6 / 3090）：
  - route 7/7、subtask 语义正确；locked-action ~0.59s，RTC 连续帧钉住验证通过。
  - arm 目标比 steps_1000 更贴近 GT（如 frame80 x=0.618≈GT0.617、gripper 0.728≈0.77），
    但仍不够准；NAV waypoint 大量饱和在 q01/q99 极值（模型偏置，后续可关注）。
- 双 expert 动作头（新分支 `robodog_two_heads`，commit 9df5f6d）：
  - QwenPI 改为 DualActionHeads（nav 3 维 waypoint head / arm 7 维 TCP head），
    route token 选择激活；trainer 的 freeze/param-group/日志经容器属性兼容。
  - 数据管线不变（10 维 action + route + mask，按 route 切维），统计沿用。
  - 服务器端 eval 冒烟通过：混合 batch forward loss、10 维 dim_loss 聚合、
    locked 推理 nav→(10,3)、grasp/place→(10,7)。
  - 注意：双 head 代码不能加载旧单 head checkpoint；server 保持 robodog 分支代码。
- GPU 现状：0/1 被他人 internvla 训练占用（勿动）；可用 A40 2/3/4，5/6 为 3090。
- waypoint 可用性量化（r3 final，120 帧 locked nav）：step0 落在数据范围
  （dx±0.52m/dyaw±0.52rad）的比例 **0/120 = 0%**——当前模型 waypoint 完全不能用，
  根因是 pooled 归一化把 step0 目标压进窄区间、模型采样 clip 到 ±1 反归一化成极值。
- 关节/动作审计：全部关节在 X5 限位内；**joint5 几乎不动**（std≈0，疑似采集锁死，
  待查）；步间速度合理。抓取探测：arm 目标 100% 在 workspace 内，但首个 TCP 目标
  中位误差 0.171m（罐径仅 66mm），当前模型抓取精度不够直接用。
- **夹爪语义修复**：训练目标夹爪维原为实测指位（`lerobot_dataset.py` 用关节均值），
  抓握时停在物体宽度 0.77，导致模型学出"半开"目标、实机指令过宽抓不住；
  改为使用 `control.action` 的夹爪指令（0=闭,1=开），验证新目标闭合帧占比 20.6%。
- 双 head 训练启动排障（r4/r5/r6）：
  - accelerate 默认抢 GPU 0/1（被 xch 占用）→ `CUDA_VISIBLE_DEVICES=2,3,4`；
  - `_build_dual_action_heads` 对 AccessTrackedConfig 的 OmegaConf 报错 → 转 plain dict；
  - NCCL 600s 超时（数据 worker 卡死）→ 恢复 num_workers=4、b14/卡；
  - r6 用夹爪指令目标 + per-step 归一化重启（4649 步，ETA ~7.7h）。

## 全日总结

- r3 训练完成并复测，动作质量较 steps_1000 有提升；双 head 架构开发完成并通过冒烟。
- 定位并修复两个数据问题：①waypoint pooled 归一化（改 per-step）；②夹爪目标用实测
  指位（改 control.action 指令）。r6 双 head 训练已在 A40 2/3/4 跑（b14，~7.7h）。
- r3 模型 waypoint 0/120 不可用、抓取中位误差 17cm 不宜实机；等 r6 完成后再闭环验证。
