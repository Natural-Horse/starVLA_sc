# 2026-08-03 工作日志

## 工作记录
- 校验 `liangzhu_0729_n250`：原始 250 条中 243 条成功，7 条失败被正确排除；LeRobot 为 243 episode、56,390 帧、5 Hz，平均 46.41 秒/条。243 个 parquet、front/wrist 共 486 个视频、17D state、10D action、索引/时间戳/终止标记、NaN/Inf、视频帧数和逐帧 instruction 均通过本地全量检查，当前 StarVLA loader 可生成方向增强 global instruction 与正确的 NAV/机械臂双 mask。
- 仅同步训练必需的 `data/meta/videos/validation_report` 和 manifest 到 `zju-server:/hdd4/MaTianran/pct_workspace/starVLA_sc/datasets/liangzhu_0729_n250`，省略 5 GB episode 调试副本；训练文件 737 个、1,763,702,063 字节，rsync dry-run 待传 0。远端再次打开全部 parquet 和 486 个视频，结果为 243 episode、56,390 帧、0 错误。
- 新增 `scripts/train_go2_vlm5_then_action_tmux.sh`：从 n200 最终 VLM 权重续训 5 epoch，成功后依次训练 NAV 和 GRASP/PLACE action 各 1 epoch；任一阶段失败即停止后续阶段，同一 tmux 内启动 TensorBoard，每 1000 step 保存。提交 `a41dca2` 已推送并部署到远端；本地 dry-run、`bash -n`、`git diff --check` 及 Go2 测试 `22 passed, 3 skipped`。
- 远端 dry-run：90% train split 下 VLM 27,175 样本/epoch、5 epoch 共 67,940 step；NAV 15,017 样本/7,509 step；GRASP/PLACE 10,414 样本/5,207 step。旧 VLM final 权重 11,920,536,322 字节且 zip 有效；A40 3/4 空闲、TensorBoard 6016 未占用，正式训练未启动。
- 增补 Fork-only 规则：`gx-real` 只允许向 `Natural-Horse/gx-real` 的 `origin` 提交/推送/PR，`upstream` 只读。skill 与三仓 NOTICE 已更新；`gx-real` `6fbb70d`、`pct_scene` `de73d13`、StarVLA `85537eb` 均只推送到用户 Fork。

## 全日总结
- n250 的 243 条成功数据已完成本地、传输一致性及远端可解码验证，可用于当前 10 维 route-conditioned 训练。
- VLM 续训 5 epoch 后自动衔接 NAV、GRASP/PLACE action 的 tmux 脚本已提交、部署并通过远端 dry-run；正式训练尚未启动。
- `gx-real` 后续只使用 `Natural-Horse` Fork，禁止向 upstream 推送或创建 PR。
