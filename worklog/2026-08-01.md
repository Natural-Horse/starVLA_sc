# 2026-08-01 工作日志

## 工作记录

- 核验 n200：130 episode、30,200 帧；抽样后 16,170 条，训练/验证为 14,549/1,621。修复多任务 instruction 错配及路由、subtask 指标和训练配置检查。
- 完成 VLM 路由、NAV Flow Matching 动作头、GRASP/PLACE 路由校准三阶段 tmux 编排；五张 A40、ZeRO-3、无 CPU offload、单卡 batch 12，对应 243/134/93 step。
- VLM 在 243/243 保存后仅 NCCL 清理阶段 OOM，最终权重有效；将训练成功后的清理异常降为 warning，并把短阶段非法 warmup 自动回退到约 10%。
- 从 VLM 成品续跑后全部完成：VLM 约 21 分钟、NAV 约 8--9 分钟、pick/place 约 8 分钟。三个 11.92 GB checkpoint 均通过 zip 结构检查，最终权重位于 `go2_n200_curriculum_a40_b12_0801_pick_place/final_model/pytorch_model.pt`；TensorBoard 6006 仍运行。
- 新增 `deployment/go2_remote`：回环限定的版本化服务、严格请求/输出校验、QwenPI `predict_typed_action` 后端和 mock 后端；训练 prompt、双相机顺序及 `[1,3]` 机体系速度与数据集保持一致。
- 新增远端推理 tmux 启动脚本，GPU 有进程时拒绝启动。跨仓库 mock 真连接通过，Go2 相关 23 项测试通过；真实模型服务待用户确认评测服务器后启动。
- 更新并校验 `starvla-robodog` skill：模型输出语义归 `starVLA_sc`，世界系变换、DWA/RL 速度适配及 cuRobo 执行归 `pct_scene`。

## 全日总结

- n200 数据及监督配置完成复核，多任务 instruction 问题已修复。
- 三阶段训练在 `zju-server` 五张 A40 上完成，最终完整模型可用；修复了保存后 NCCL 清理 OOM 与短阶段 warmup 配置。
- 完成回环远程推理服务、版本协议、tmux 启动器和 mock 联调，23 项 Go2 测试通过。
- skill、双仓库职责、同步和中文日志规范已更新并校验。
