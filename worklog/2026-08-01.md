# 2026-08-01 工作日志

## 工作记录

- 将训练统一改为 tmux 托管，新增全流程顺序编排与 TensorBoard；按训练集和全局 batch 自动计算一轮 step，五卡 batch 12 下 VLM/NAV/pick-place 分别为 243/134/93 step。
- 检查 Go2 全部训练配置与数据监督配对；修复 dataloader 将所有样本主任务 instruction 固定为 `tasks.jsonl` 第一条的问题，并补充多任务及配置一致性测试。
- 更新并校验 `starvla-robodog` skill：远程任务需确认服务器，训练必须使用 tmux，`robotdiff_new` 支持 rsync 与本机反向 SOCKS；文档及每日单日志统一中文。
- 核验 n200：130 个有效 episode、30,200 原始帧；抽样后共 16,170 条，训练集 117 episode/14,549 条，验证集 13 episode/1,621 条。
- 修复并校验 Go2 训练配置：DeepSpeed 与 YAML 统一使用 5.0 梯度裁剪，验证集关闭 shuffle，训练/验证指标分名，增加路由及 subtask token 准确率。
- 新增三阶段启动脚本：先训练 VLM 路由与 subtask，再冻结 VLM 训练 Flow Matching NAV waypoint，最后加载 NAV 权重并用 GRASP/PLACE 校准 VLM；动作头冻结，每 1000 step 保存并启用 TensorBoard。
- `zju-server` 的 3090+CPU offload run 因 CUDA toolkit/PyTorch 版本差异及残留 worker 导致节点高负载，已仅清理本项目 PID；改用五张空闲 A40、ZeRO-3、无 CPU offload。batch probe 2/4/8/12/14 均通过，按用户指定采用单卡 batch 12。
- 正式 tmux `go2_n200_curriculum_a40_b12_0801` 已启动：Step 20 稳定约 5.1 秒，五卡约 46.2--48.2 GiB，TensorBoard 6006 正常；`save_interval=1000`，单轮少于 1000 optimizer step，阶段结束仍保存 final model。
- `robotdiff_new` 直连 GitHub/ModelScope 正常；本机建立反向 SOCKS `127.0.0.1:1080`，代理访问两站均返回 200。
- 明确监督边界：NAV 使用离散稀疏 waypoint；GRASP/PLACE 当前只训练高层路由及固定文本，尚无连续机械臂动作监督。

## 全日总结

- 完成 skill 的服务器选择、同步、tmux、网络转发和中文日志规范更新，校验通过。
- 确认 n200 数据完整可训练：130 episode、30,200 帧、16,170 条路由监督样本。
- 修复多任务 instruction 错配，补齐路由过滤、阶段约束、指标和配置检查；17 项 Go2 测试通过，23 份训练 YAML 均可解析。
- 完成 VLM 全路由、NAV 动作头、GRASP/PLACE 路由校准三阶段 tmux 编排；正式训练已在 `zju-server` 五张 A40 上稳定运行，TensorBoard 端口 6006。
- `robotdiff_new` 当前可直连外网，并验证本机反向 SOCKS 备用通道可用。
