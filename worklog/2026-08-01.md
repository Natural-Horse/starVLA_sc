# 2026-08-01 工作日志

## 工作记录

- 检查 Go2 全部训练配置与数据监督配对；修复 dataloader 将所有样本主任务 instruction 固定为 `tasks.jsonl` 第一条的问题，并补充多任务及配置一致性测试。
- 更新并校验 `starvla-robodog` skill：远程任务启动前由用户确认 `zju-server`/`robotdiff_new`；后者使用本地 `rsync` 直传；说明文档与每日单日志统一中文并压缩记录。
- 只读检查 `zju-server`：GPU 5、6 两张 RTX 3090 均空闲；原训练和 TensorBoard 已退出，日志停在 `0/1000`，无 checkpoint、异常栈、OOM 或重启记录。
- 核验 n200：130 个有效 episode、30,200 原始帧；抽样后共 16,170 条，训练集 117 episode/14,549 条，验证集 13 episode/1,621 条。
- 修复并校验 Go2 训练配置：DeepSpeed 与 YAML 统一使用 5.0 梯度裁剪，验证集关闭 shuffle，训练/验证指标分名，增加路由及 subtask token 准确率。
- 新增三阶段启动脚本：先训练 VLM 路由与 subtask，再冻结 VLM 训练 Flow Matching NAV waypoint 动作头，最后低学习率联合微调；每 300 step 保存权重并启用 TensorBoard。
- 明确监督边界：NAV 使用离散稀疏 waypoint；GRASP/PLACE 当前只训练高层路由及固定文本，尚无连续机械臂动作监督。

## 全日总结

- 完成 skill 服务器选择、同步和中文日志规范更新，校验通过（72 行）。
- 确认 n200 数据完整可训练：130 episode、30,200 帧、16,170 条路由监督样本。
- 修复多任务 instruction 错配，补齐阶段约束、指标、梯度裁剪一致性和安全启动检查；15 项 Go2 测试通过，23 份训练 YAML 均可解析。
- 完成 VLM/动作头/联合微调三阶段脚本；本次未启动、停止或修改任何远程进程，远程训练仍需用户确认服务器。
