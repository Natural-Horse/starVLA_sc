# 2026-08-04 工作日志

## 工作记录

- 实现 locked-subtask action-only 推理路径并实测：
  - `QwenPI.predict_locked_action`：跳过 route/subtask 自回归生成，直接用已确定 solution 跑 action head。
  - `backend.infer` 支持 `locked_route`/`locked_subtask`：先做 first-token route 检测（~85ms），route 未变走 action-only（实测 nav 249ms / grasp 178ms，原完整推理 ~3.0s，提速约 12-16 倍）；route 变化则退回完整推理重新生成 subtask。
  - 修复 `StarVLABackend` 加载旧 checkpoint 词表 padding（151677 vs 151936）失败问题，改用 `adapt_padded_vocab_state_dict`。
  - pct_scene/gx-real 交互 client 已接入锁定逻辑：完整推理一次→锁定→后续只跑 action，arm 执行/跳过时 unlock。
  - zju-server 确认可访问 GitHub（200），已改用 `git pull --ff-only` 更新（此前网络抖动时用 rsync 兜底）。
- 新链路测评（steps_24000 checkpoint，held-out n250 eval 25 episodes / 2846 帧，GPU 5）：
  - VLM：route 99.72%、subtask 95.75%、联合 95.64%；与 VLM final 阶段一致（action 阶段冻结 Qwen，符合预期）。
  - action loss（训练 TensorBoard eval 记录，steps_24000 附近）：action_dit_loss_eval 约 0.92-1.09，dim 0/1/2（NAV）非零，dim 3-9（arm）为 0（当前 eval batch 全为 nav）。
  - 训练实时对比：action_dit_loss（train）1.13-1.58、action_dit_loss_eval 0.98-1.61（steps 24800-25700）。
- 训练进度：action 5ep 跑到 25,786/97,620（26.4%，剩余约 27.6h），最新 checkpoint steps_25000。
- 修复推理服务启动失败：`backend._load_model` 直接 `build_framework` 时未初始化
  accelerate 状态，`prepare_qwen_special_tokens` 的 accelerate logger 抛
  `RuntimeError`；在 `_load_model` 中补 `PartialState()`（与评测脚本一致）。
  zju-server 仓库此前落后 4 个提交导致 `configs/vla_eval/server.yaml` 缺失，
  已 `git pull --ff-only` 同步并保留服务器本地的 `gpu: 2` 改动。
  服务已重启并通过 WebSocket health 检查（`starvla-go2-eval/v2`，GPU 2 A40 占 11.6GB）。

## 全日总结

- locked action 推理链路实测生效（~0.25s/次 vs 3.0s），VLM 准确率与 VLM final 一致；action 训练中（26.4%，剩余约 27.6h）。
- 推理服务修复 accelerate 初始化问题后已在 zju-server 正常启动并通过 health 检查。
- 待现场：Isaac 环境联调（启动场景 + 重置 + 人工门控跑通）、cuRobo 抓放接入。
