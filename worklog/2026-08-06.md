# 2026-08-06 工作日志

## 工作记录

- r8 训练完成：`go2_singlehead_perstep_nortc_3a40_0805_r8`（3254 步 / 5 epoch /
  单 head / per-step 归一化 / 夹爪指令目标 / 无 RTC），final_model 22:17 保存。
- 推理 server 切 r8 final（GPU 6 / 3090），对 VLM/action 做测试：
  - VLM：7/7 subtask 语义正确（VLM 冻结，稳定）；
  - waypoint：step0 落在数据范围内比例 r3 的 **0% → r8 的 77.5%**
    （dx 86.7% / dy 100% / dyaw 88.3%）——per-step 归一化修复生效；
  - 抓取 TCP：target0 中位误差 0.169m（r3 0.183m）——几乎无改善，
    确认机械臂精度是训练量问题（5 epoch 欠拟合），需继续加训。
- 部署链路验证（真实相机闭环）：隧道两端正常；robodog 上
  `publish_real_cameras.py` 发布 front/wrist JPEG，`run_vla_real_interactive.py`
  经隧道连 r8 server，真实图像推理输出 route/subtask/waypoint 连续正常。
- r8 大样本输出分布探测（`/tmp/dist_probe.py`，300 nav + 260 arm 帧逐帧独立）：
  - nav：step0 落在数据范围内 **81.9%**（dx q01/q99 [-0.249, 0.518] 与数据
    [-0.249, 0.518] 对齐；dyaw q01/q99 [-0.511, 0.516] 对齐）；step0 平移
    q50/q90/q99 = [0.249, 0.519, 0.531]m，均值推理 598ms；
  - arm：workspace 内比例 100%；TCP 位置误差 q25/q50/q90 =
    [0.115, 0.173, 0.264]m；姿态误差 q50≈2.63rad（接近均匀分布，基本不可用）；
    gripper q10/q50/q90 = [0.0, 0.44, 1.0]（均值趋中，二值方向仍不确定）。
    结论：waypoint 已可部署；机械臂位置/姿态均为欠拟合，需继续加训。

## 全日总结

- r8 完成：waypoint 数据问题修复得到验证（0%→77.5%），VLM/链路正常；
  TCP 精度仍是训练量问题（0.169m），下一步加训 epoch。
- 大样本确认：waypoint step0 in-range 81.9%（数据对齐），机械臂 TCP 欠拟合
  （0.173m/姿态不可用），gripper 输出趋中需更多训练。
